#parse ( "/include/domain_info.vm" )
package ${ROOT_PKG}.core.${entity.name.toLowerCase()}.gateway;

import ${ROOT_PKG}.core.${entity.name.toLowerCase()}.domain.${entity.name};
import ${ROOT_PKG}.core.common.paging.Page;

#include ( "/include/author.vm" )
#set ( $fieldNameAndKey = $strUtils.substringBetween($target.file, "By", "Gateway") )
#set ( $uncapitalizedFieldNameAndKey =  $wordUtils.uncapitalize($fieldNameAndKey) )
@FunctionalInterface
public interface Search${pluralEntityName}By${fieldNameAndKey}Gateway {

    Page<${entity.name}> execute(final Long ${uncapitalizedFieldNameAndKey}, final Integer pageNumber, final Integer size, final String orderBy);
}
