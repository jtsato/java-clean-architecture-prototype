Book {
 id : long { @Id };
 author : Author ;
 title : string { @NotBlank };
 price : decimal { @NotNull } ;
 available : boolean { @NotNull } ;
 createdDate : timestamp { @DefaultValue("now") } ;
 lastModifiedDate : timestamp { @DefaultValue("now") } ;
}
